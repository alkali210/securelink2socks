# 开发约定

- 当前功能与使用方式见 `README.md`，开发流程见 `docs/development.md`，阶段计划见 `docs/roadmap.md`。历史验证记录不代表新增功能已通过验收。
- 保持纯用户态数据面：本程序不创建 TUN/TAP、不修改主机路由或 DNS；SOCKS 仅监听 `127.0.0.1`。流量选择交给 Mihomo 等外部客户端。
- ACL 默认拒绝，未知或畸形规则不得扩大权限；断线、重连或权限更新时撤销旧授权与连接。校园请求失败不得回退主机直连。
- 目标域名仅通过当前 VPN 下发的 DNS 解析，不回退系统或公网 DNS；域名授权不得转为通用 IP 授权。保持控制面、隧道、ACL 与 SOCKS 的职责分离。
- `third_party/go-openvpn` 是固定提交的 fork 子模块。依赖修改须在 fork 中连同测试独立提交并推送，再更新主项目引用；保留上游许可证，不改写已被引用的提交。
- 代码改动运行相关测试及 `go vet ./...`；依赖改动另运行 `go -C third_party/go-openvpn test -parallel 1 -timeout 5m ./...`。纯文档改动检查链接和 `git diff --check` 即可。
- 普通测试使用合成数据，不访问 XMU。实机验证仅使用用户授权的账号和资源，显式开启 `SECURELINK2SOCKS_E2E`；公共 CI 不运行实机测试。不记录或提交 token、Cookie、私钥、完整回调 URL、会话文件及含凭据的原始协议数据。
- README 保持简短；实现细节、排障和验证证据放在 `docs/`。涉及行为、配置或依赖维护方式的修改同步更新文档，明确已验证范围与待验证项。
